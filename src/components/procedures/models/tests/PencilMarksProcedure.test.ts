import {PencilMarksProcedure} from "../PencilMarksProcedure";

describe('PencilMarksProcedure', () => {
    describe('conductor', () => {
        it('can build the pencil marks procedure', () => {

        })
    })

    describe('steps', () => {
        it('', () => {

        })
    })
})

// steps add cell
// apply rules
//     remove numbers that exist in the square
//     remove numbers that exist in the row
//     remove numbers that exist in the column